<?xml version="1.0" encoding="UTF-8"?>

<databases>
	<database id="default">
		<property name="host" value="127.0.0.1"></property>
		<property name="port" value="6379"></property>
		<property name="auth" value="passwd"></property>
		<property name="max_active" value="1024"
			desc="可用连接实例的最大数目，如果赋值为-1，则表示不限制；如果pool已经分配了maxActive个jedis实例，则此时pool的状态为exhausted(耗尽)。"></property>
		<property name="max_idle" value="200"
			desc="控制一个pool最多有多少个状态为idle(空闲的)的jedis实例，默认值也是8。"></property>
		<property name="max_wait" value="10000"
			desc="等待可用连接的最大时间，单位毫秒，默认值为-1，表示永不超时。如果超过等待时间，则直接抛出JedisConnectionException；"></property>
		<property name="timeout" value="10000"></property>
		<property name="test_on_borrow" value="true"
			desc="在borrow一个jedis实例时，是否提前进行validate操作；如果为true，则得到的jedis实例均是可用的；"></property>
	</database>
</databases>